프로그래밍은 컴퓨터에게 일을 시키는 것 (코딩도 마찬가지)
컴퓨터에 명령을 하려면 번역기가 필요한데 그걸 인터프리터 or 컴파일러 라고 함
파이썬을 쓰는 이유
-쉽고 간결해서
-자바나 C보다 최근에 만들었기 때문
-인공지능, 데이터 분석 같은 것에 사용됨
print(A) A = 문자열
end: print 기본: 줄 바꿈
sep: print 기본: 공백
print ("hello") print ("world")
-hello world
print("hello", "world", sep = ',')
-hello,world
input() 표준입력 - 키보드 입력
input() 함수의 반환값은 문자열
input()
input ("이름 : ")
-결과
123
이름 : 홍길동
이스케이프 시퀀스
-백슬래시 \
\n : 줄 바꿈
\t : 탭
\' : ' 표시
\" : " 표시
변수 : 값을 저장하는 공간 (메모리에)
변수 값은 언제든지 변할 수 있음
덮어씨워지는 특성이 있음
'=' 은 같다라는 뜻이 아니라 오른쪽에 있는 값을 왼쪽에 있는 변수에 저장하라는 뜻
정수(int) 실수(float) 문자열(str) 부울(bool)
포맷 코드 : 출력하려는 형태를 결정하는 것 
%d = 정수 %f = 실수 %c = 문자 %% = 문자 % 자체
포맷 함수를 통해서도 가능
산술 연산자 - +, -, *(곱하기), /(나눗셈), // (버림 나눗셈), %(나머지), **(거듭제곱)
형변환 : 자료의 형태를 변경
int(data) 정수형으로 변경
str(data) 문자열로 변경
float(data) 실수형으로 변경
a = int(input())
import를 통해서 외부에서 불러올 수 있다
random() 임의의 실수를 반환
random (a, b) a에서 b 중 임의의 실수를 반환
an로 별칭을 붙일 수 있음
from으로 모듈 내의 특정 변수나 함수를 가져올 수 있음
